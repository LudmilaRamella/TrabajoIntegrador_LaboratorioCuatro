<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container-fluid px-4">
    <!-- Título / Nombre del sistema -->
    <a class="navbar-brand fw-bold" href="/">
      Carrito Online
    </a>

    <!-- Botón para dispositivos móviles -->
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Contenido del navbar -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav align-items-lg-center gap-lg-3">

        {{#if user}}
          <!-- 🔐 Usuario logueado -->
          <li class="nav-item">
            <a href="/" class="nav-link {{#if activeHome}}active{{/if}}">Inicio</a>
          </li>
          <li class="nav-item">
            <a href="/productos" class="nav-link {{#if activeProductos}}active{{/if}}">Productos</a>
          </li>
          <li class="nav-item">
            <a href="/categorias" class="nav-link {{#if activeCategorias}}active{{/if}}">Categorías</a>
          </li>
          <li class="nav-item">
            <a href="/catalogo" class="nav-link {{#if activeCatalogo}}active{{/if}}">Catálogo</a>
          </li>

          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle d-flex align-items-center"
              href="#"
              id="userDropdown"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              <i class="bi bi-person-circle me-1"></i>
              {{user.nombre}} {{user.apellido}}
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow-sm" aria-labelledby="userDropdown">
              <li><a class="dropdown-item" href="/perfil"><i class="bi bi-person me-2"></i>Perfil</a></li>
              <li><a class="dropdown-item" href="/carrito"><i class="bi bi-cart3 me-2"></i>Mi Carrito</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout"><i class="bi bi-box-arrow-right me-2"></i>Salir</a></li>
            </ul>
          </li>

        {{else}}
          <!-- 👤 Invitado -->
          <li class="nav-item">
            <a href="/login" class="btn btn-outline-light px-3 py-1 me-2">Iniciar sesión</a>
          </li>
          <li class="nav-item">
            <a href="/registro" class="btn btn-primary px-3 py-1">Registrarse</a>
          </li>
        {{/if}}

      </ul>
    </div>
  </div>
</nav>
